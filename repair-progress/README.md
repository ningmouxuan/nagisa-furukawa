# 古河渚宠物修图进度

这个目录保存当前这一轮动作统一修图的阶段性成果，方便在 GitHub 上直接查看，不把大量中间接口响应一并提交。

## 当前标准

- 统一标准以原始成品里的 `idle` 坐下动作形象为主
- `running` 动作组目前判断已经比较接近目标，暂不优先修改
- 当前重点是把 `jumping`、`failed / waving`、`review` 往同一套人物形象继续拉齐

## 当前已整理候选

- `waiting-approved.png`
  - 当前用户明确表示“这个还行”的 `waiting` 版本
- `jumping-match-seated-v4.png`
  - 让 `jumping` 更靠近坐下组形象的候选
- `failed-current.png`
  - 当前 `failed` 可继续作为表情组基准之一
- `waving-current.png`
  - 当前 `waving` 可继续作为表情组待修基准之一
- `review-current.png`
  - 当前 `review` 候选，后续还需要继续按坐下组标准复核

## 说明

- 这里提交的是“当前进度快照”，不是最终定稿
- 原始工作目录里还有更多尝试图、预览图和接口响应文件，默认不进仓库
